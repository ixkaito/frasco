language: ruby
rvm:
  - 2.2

branches:
  only:
  - master

before_install:
  - npm install

install: gem install jekyll html-proofer

script: npm run travis

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

sudo: false
